#compdef rwc

autoload -U is-at-least

_rwc() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" \
'-b[Show the count of bytes]' \
'--bytes[Show the count of bytes]' \
'-c[Show the count of chars]' \
'--chars[Show the count of chars]' \
'-w[Show the count of words]' \
'--words[Show the count of words]' \
'-l[Show the count of lines]' \
'--lines[Show the count of lines]' \
'-L[Show the length of the longest line]' \
'--longest-line[Show the length of the longest line]' \
'-h[Print help information]' \
'--help[Print help information]' \
'-V[Print version information]' \
'--version[Print version information]' \
'*::paths -- The path(s) you should provide:_files' \
&& ret=0
}

(( $+functions[_rwc_commands] )) ||
_rwc_commands() {
    local commands; commands=()
    _describe -t commands 'rwc commands' commands "$@"
}

_rwc "$@"
